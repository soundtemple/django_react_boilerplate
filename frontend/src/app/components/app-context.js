import React from "react";

// import AppContext from "../../app/components/app-context";
// usage = const { updateUser } = React.useContext(AppContext);

const AppContext = React.createContext({});

export default AppContext;
